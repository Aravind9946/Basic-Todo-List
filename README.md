# Basic Todo App

This is a basic todo app build using React. It have the below mentioned functionalities:

**Add new row to the list**
**Delete an existing row from the list**
**Check or uncheck the rows which means it as completed or not**
